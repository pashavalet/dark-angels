{
  "name": "dark-angels",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "test": "pnpm -r test",
    "test:watch": "pnpm --parallel -r test:watch"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/ws": "^8.18.1"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
